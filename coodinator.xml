<coodinator-app
    name="${coodinatorName}"
    frequency="${coord:days(1)}"
    start="${start_date}"
    end="${end_date}"
    timezone="${timezone}"
    xmlns="uri:oozie:coordinator:0.3">
<action>
    <workflow>
        <app-path>${app_path}</app-path>
    </workflow>
</action>
</coodinator-app>


